# 本地回归数据策略

| 项目 | 策略 |
|---|---|
| 采集内容 | 保留窗口内原始内容，不做脱敏 |
| 目标范围 | 只采集 `session_meta.cwd` 位于本次任务目标内的会话 |
| 窗口边界 | 窗口外只保存计数和前后事件布尔值，不复制正文 |
| 配置 | 不保存任何任务目标 |
| Git | `.sdd-frl/runs/` 默认忽略 |

例如任务目标为 `C:\work\product-a`，会话目录为
`C:\work\product-a\nested` 时可以采集；`C:\work\product-b` 的会话不会进入该任务。
