{
  "name": "failure-review-loop",
  "version": "0.1.0",
  "private": true,
  "description": "Evidence-backed task outcome, efficiency, and attainment review pipeline for Codex sessions",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "validate:examples": "uv run sdd-frl validate-examples"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
